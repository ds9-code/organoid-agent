"""Per-task scientific evaluations for the spreadsheet tasks.

Each module is self-contained: load HNOCA, set up the prediction problem,
run baselines + agent-callable tool, compute metrics, write a CSV.
"""
