numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
anndata>=0.10
scanpy>=1.9.3
pyyaml>=6.0
pytest>=7.4

# Streaming subset download (HTTP-range reads against Zenodo h5ad)
h5py>=3.10
fsspec>=2024.0
aiohttp>=3.9
requests>=2.31

# Agent layer
openai>=1.40
python-dotenv>=1.0

# MCP server (so Hermes-CLI / Claude Desktop / Cursor can call HNOCA tools)
mcp>=1.0

# Optional / heavy — uncomment when needed
# scvi-tools>=1.0
# torch>=2.1
# moscot>=0.3
